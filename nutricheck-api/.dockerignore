# Everything the image does not need. The build context is this directory, so
# the mobile app and the shared docs are already outside it.
node_modules
**/node_modules
**/dist
**/build
**/coverage
**/.turbo
**/*.tsbuildinfo

.git
.github
.gitignore

apps/mobile

**/*.test.ts
**/*.spec.ts
**/__tests__

.env
.env.*
!.env.example

deploy
*.md
Dockerfile*
docker-compose*.yml
